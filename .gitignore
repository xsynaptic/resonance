# build output
dist/

# generated types and caches
.astro/
.cache/
.wrangler/
__pycache__/
/worker-configuration.d.ts

# dependencies
node_modules/

# logs
*-debug.log*
astro-build.jsonl

# environment variables
.env
.env.*
!.env.example
!deploy/.env.example

# Worker secrets for `wrangler dev`
.dev.vars
.dev.vars.*

# certs
deploy/certs

# macOS
.DS_Store

# local editor / tooling
.claude/reference
.claude/tasks*
.claude/settings*.json
.claude/worktrees/
.impeccable/

# content lives within a private repo; no trailing slash, so the worktree symlink matches too
/packages/content

# database backups (large, not for VCS)
backups/


# temporary machinery: the legacy download recount, kept until launch confirms the numbers
packages/scripts/src/backfill/
