# build output
dist/

# generated types and caches
.astro/
.cache/
.wrangler/
__pycache__/
/worker-configuration.d.ts

# dependencies
node_modules/

# logs
*-debug.log*
astro-build.jsonl

# macOS
.DS_Store

# environment variables
.env
.env.*
!.env.example
!deploy/.env.example

# Worker secrets for `wrangler dev`
.dev.vars
.dev.vars.*

# deploy details
deploy/certs
deploy/nginx/

# local editor / tooling
.claude/reference
.claude/tasks*
.claude/settings*.json
.claude/worktrees/
.impeccable/

# content lives within a private repo; no trailing slash, so the worktree symlink matches too
/packages/content

# testing
packages/player/e2e/.fixtures/
packages/player/playwright-report/
packages/player/test-results/
temp/

# database backups (large, not for VCS)
backups/
