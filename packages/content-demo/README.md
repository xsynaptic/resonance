# Demo content

Example content to show the shape of what the private content repository contains. Set `CONTENT_DATA_PATH=packages/content-demo` in the root `.env` to build.

Note: there is no audio or artwork, so a mix renders without the player and pages render without images.
