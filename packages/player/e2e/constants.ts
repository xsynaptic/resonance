export const pagePort = 4183;

// A second port is a second origin, so every stream is cross-origin as in production
export const fixturePort = 4184;
export const fixtureOrigin = `http://localhost:${String(fixturePort)}`;

export const seekSeconds = 30;
