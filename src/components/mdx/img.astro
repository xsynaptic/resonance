---
import MediaImage from '#components/parts/media-image.astro';

interface Props {
	alt?: string | undefined;
	id: string;
}

const { alt = '', id } = Astro.props;
---

<MediaImage
	alt={alt}
	class="my-6 block rounded"
	path={id}
	sizes="(min-width: 48rem) 48rem, 100vw"
	width={1200}
/>
