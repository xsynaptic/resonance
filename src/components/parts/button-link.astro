---
import type { HTMLAttributes } from 'astro/types';

import type { IconId } from '#lib/utils/icon-types.ts';

type ButtonVariant = 'small' | 'standard';

interface Props extends HTMLAttributes<'a'> {
	icon?: IconId | undefined;
	variant?: ButtonVariant | undefined;
}

const { class: className, icon, variant = 'standard', ...props } = Astro.props;
---

<a class:list={['button-x', 'prose-escape', `button-${variant}`, className]} {...props}>
	{
		icon ? (
			<svg aria-hidden="true" class="button-icon">
				<use href={`#icon:${icon}`} />
			</svg>
		) : undefined
	}
	<slot />
</a>
