---
interface Props {
	heading: string;
}

const { heading } = Astro.props;
---

<div class="flex flex-wrap items-baseline gap-x-1.5 gap-y-1 text-sm">
	<span class="font-medium text-ink-500">{heading}:</span>
	<slot />
</div>
