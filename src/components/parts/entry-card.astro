---
import type { HTMLAttributes } from 'astro/types';

import MediaImage from '#components/parts/media-image.astro';
import { formatDate } from '#lib/utils/date.ts';

interface Props extends HTMLAttributes<'a'> {
	date?: Date | undefined;
	href: string;
	image?: string | undefined;
	imageAspect?: '3/2' | 'square' | undefined;
	subtitle?: string | undefined;
	title: string;
}

const {
	class: className,
	date,
	href,
	image,
	imageAspect = 'square',
	subtitle,
	title,
	...props
} = Astro.props;

// Album artwork is square by default; opt into 3:2 per surface
const aspectClass = imageAspect === 'square' ? 'aspect-square' : 'aspect-[3/2]';
---

<a
	class:list={[
		'group block h-full rounded-lg border border-surface-700 bg-surface-600 p-2',
		className,
	]}
	href={href}
	{...props}
>
	{/* The frame holds the card's shape when an original is missing, which is routine in dev */}
	{
		image ? (
			<div class={`mb-2.5 ${aspectClass} w-full overflow-hidden rounded-xs bg-surface-900`}>
				<MediaImage
					alt=""
					class="h-full w-full object-cover"
					layout="none"
					path={image}
					width={640}
				/>
			</div>
		) : undefined
	}
	<span
		class="block font-display text-xl font-bold text-ink-100 transition group-hover:text-ink-50 group-focus-visible:text-ink-50"
		>{title}</span
	>
	{subtitle ? <span class="mt-1 block text-sm text-ink-600">{subtitle}</span> : undefined}
	{
		date ? (
			<time class="mt-1 block text-xs text-ink-700" datetime={date.toISOString()}>
				{formatDate(date)}
			</time>
		) : undefined
	}
</a>
