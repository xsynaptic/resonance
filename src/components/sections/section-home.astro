---
import type { ContentItem } from '#lib/catalog/catalog-data.ts';

import EntryCard from '#components/parts/entry-card.astro';

interface Props {
	heading: string;
	items: Array<ContentItem>;
	listHref: string;
	listLabel: string;
}

const { heading, items, listHref, listLabel } = Astro.props;
---

<section class="mb-12">
	<div class="mb-4 flex items-baseline justify-between">
		<h2 class="text-h2">{heading}</h2>
		<a class="text-meta text-highlight-500 hover:text-highlight-400" href={listHref}>{listLabel}</a>
	</div>
	<ul class="grid gap-4 sm:grid-cols-2">
		{
			items.map((item) => (
				<li>
					<EntryCard
						date={item.date}
						href={item.url}
						image={item.image}
						subtitle={item.subtitle}
						title={item.title}
					/>
				</li>
			))
		}
	</ul>
</section>
