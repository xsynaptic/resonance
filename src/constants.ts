// Path to content collections from the project root
export const CONTENT_COLLECTIONS_PATH = './packages/content/collections';

// Download counts pulled from the file server via `pnpm stats-pull`; may not exist locally
export const DOWNLOADS_STATS_PATH = './packages/content/downloads.json';

// Cards per page on the paginated collection and taxonomy archives
export const ARCHIVE_PAGE_SIZE = 24;
