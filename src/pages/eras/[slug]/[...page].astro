---
import type { InferGetStaticPropsType } from 'astro';

import TermArchive from '#layouts/term-archive.astro';
import { getErasIndex } from '#lib/collections/taxonomy/taxonomy-data.ts';
import { createTermArchivePaths } from '#lib/collections/taxonomy/term-routes.ts';

export const getStaticPaths = createTermArchivePaths('eras', getErasIndex);

type Props = InferGetStaticPropsType<typeof getStaticPaths>;
---

<TermArchive kind="Era" {...Astro.props} />
