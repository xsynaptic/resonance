---
import MainSite from '#components/main/main-site.astro';
import HomeSection from '#components/sections/section-home.astro';
import { getContentItems } from '#lib/catalog/catalog-data.ts';
import { site } from '#lib/site.ts';

const [mixes, reviews, posts] = await Promise.all([
	getContentItems('mixes'),
	getContentItems('reviews'),
	getContentItems('posts'),
]);

const mixItems = mixes.slice(0, 4);
const reviewItems = reviews.slice(0, 4);
const postItems = posts.slice(0, 4);
---

<MainSite>
	<section class="mb-12">
		<h1 class="title-main">{site.title}</h1>
		<p class="mt-2 content-measure text-ink-300">{site.description}</p>
	</section>
	<HomeSection heading="Latest Mixes" items={mixItems} listHref="/mixes" listLabel="All mixes" />
	<HomeSection
		heading="Latest Reviews"
		items={reviewItems}
		listHref="/reviews"
		listLabel="All reviews"
	/>
	<HomeSection heading="Recent Posts" items={postItems} listHref="/blog" listLabel="All posts" />
</MainSite>
