---
import type { InferGetStaticPropsType } from 'astro';

import EntryList from '#layouts/entry-list.astro';
import { createEntryListPaths } from '#lib/collections/collection-routes.ts';
import { t } from '#lib/i18n/i18n-strings.ts';

export const getStaticPaths = createEntryListPaths('mixes');

type Props = InferGetStaticPropsType<typeof getStaticPaths>;
---

<EntryList
	description={t('collection.mixes.description')}
	title={t('collection.mixes.title')}
	{...Astro.props}
/>
