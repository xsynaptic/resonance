---
import TermList from '#layouts/term-list.astro';
import { t } from '#lib/i18n/i18n-strings.ts';
---

<TermList
	collection="styles"
	description={t('collection.styles.description')}
	hasCounts={true}
	title={t('collection.styles.title')}
/>
