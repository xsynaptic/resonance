{
	"extends": "astro/tsconfigs/strictest",
	"include": ["**/*.ts"],
	"compilerOptions": {
		"erasableSyntaxOnly": true,
		"lib": ["ESNext"],
		// `node` is for the tests alone, which drive a real SQLite; the Worker itself uses no Node API
		"types": ["../worker-configuration.d.ts", "node"]
	}
}
